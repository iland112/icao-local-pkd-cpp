@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(OpenSSL REQUIRED)
find_dependency(spdlog REQUIRED)
find_dependency(jsoncpp REQUIRED)

# Include targets
if(NOT TARGET icao::icao9303)
    include("${CMAKE_CURRENT_LIST_DIR}/icao-icao9303-targets.cmake")
endif()

check_required_components(icao-icao9303)
